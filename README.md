# 🇯🇵 Discover Amazing Places in Japan

Красивый и адаптивный лендинг для туристического сервиса Odigo. Сайт посвящен путешествиям по Японии, поиску лучших локаций и туров.

Проект демонстрирует современные подходы к веб-разработке, включая семантическую верстку, CSS Grid/Flexbox и работу с нативным JavaScript.

## 📋 Особенности проекта

* **📱 Полная адаптивность:** Сайт корректно отображается на всех устройствах — от широкоформатных мониторов до мобильных телефонов (брейкпоинты: 960px, 767px, 500px).
* **🎨 Современный CSS:** Использование `Grid Layout` и `Flexbox` для построения сложных сеток контента.
* **⚡ JavaScript без библиотек:** Вся интерактивность написана на чистом JS (Vanilla ES6).
* **⚓ Плавная навигация:** Реализован скролл к якорным ссылкам с анимацией.
* **🍔 Мобильное меню:** Полноценное бургер-меню с анимацией открытия и автоматическим закрытием при выборе пункта.
* **🎬 Интерактивный хедер:** "Шапка" сайта меняет цвет фона при прокрутке страницы.

## 🛠 Технологический стек

* **HTML5:** Семантическая разметка (`<header>`, `<main>`, `<section>`, `<nav>`, `<footer>`).
* **CSS3:**
    * Методология именования классов (BEM).
    * Анимации `transition` и `transform`.
    * Reset CSS для кроссбраузерности.
    * Подключены локальные шрифты (Lato).
* **JavaScript:** Модульная структура (IIFE), работа с DOM, событиями `scroll` и `click`.

##🚀 Как запустить Поскольку это статический веб-сайт, установка зависимостей или использование `npm` не требуются.

1. **Скачайте проект** на свой компьютер.
2. Убедитесь, что в папке `img` находятся все необходимые ресурсы (логотипы, фоновые изображения).
3. Просто откройте файл `index.html` в любом современном браузере (Chrome, Firefox, Safari).

##🧩 Функционал JavaScriptВ файле `main.js` реализованы три ключевые функции:

1. **Sticky Header:** Отслеживает прокрутку (`window.onscroll`) и добавляет класс `.header_active`, когда пользователь скроллит вниз более чем на 50px.
2. **Burger Menu:** Обрабатывает клики по иконке бургера и крестику, а также закрывает меню при клике на любую ссылку навигации (удобно для mobile UX).
3. **Smooth Scroll:** Кастомная функция плавной прокрутки до секций сайта, использующая `requestAnimationFrame` для максимальной производительности.


    
